PKG_CPPFLAGS = `gsl-config --cflags` $(SHLIB_OPENMP_CXXFLAGS)
## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = `gsl-config --libs` $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
